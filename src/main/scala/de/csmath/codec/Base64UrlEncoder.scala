package de.csmath.codec;

import Codec._


/**
 *  The class Base64UrlEncoder provides operations to encode a
 *  traversable collection of bytes with URL save symbols.
 */
class Base64UrlEncoder extends Base64FileEncoder with UrlPadding { }
